### **Это приложение для контроля потребляемой памяти.**

**Для работы приложения необходимо установить все зависимости из файла "requirements.txt"** (`python pip install -r requirements.txt`) **и запустить файл "main.py"**.
